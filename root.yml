---
- hosts: all
  gather_facts: false
  tasks:
   - name: List all files in root
     command: "ls /root"
     register: rootout

   - name: Debug roott
     ansible.builtin.debug:
       var: rootout

   - name: Debug CMD
     ansible.builtin.debug:
       var: rootout.cmd
